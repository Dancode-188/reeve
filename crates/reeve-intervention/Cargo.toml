[package]
name = "reeve-intervention"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Reeve's control plane: command dispatch, acks, and the audit log. Internal; its API may change in any release."
readme = "README.md"

[lints]
workspace = true

[dependencies]
reeve-model = { path = "../reeve-model", version = "1.1.0" }
reeve-storage = { path = "../reeve-storage", version = "1.1.0" }
tokio = { workspace = true }
tokio-stream = "0.1"
tonic = { workspace = true }
tonic-prost = "0.14"
prost = "0.14"
tracing = { workspace = true }

[build-dependencies]
tonic-prost-build = "0.14"
