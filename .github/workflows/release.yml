name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build release binary
        run: cargo build --release
      - name: Publish release binary
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload ${{ github.ref_name }} target/release/reeve --clobber 2>/dev/null || \
          gh release create ${{ github.ref_name }} \
            --title "Reeve ${{ github.ref_name }}" \
            --notes "See [CHANGELOG.md](https://github.com/Dancode-188/reeve/blob/main/CHANGELOG.md) for details." \
            target/release/reeve
